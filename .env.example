# 复制为 .env 后填写，勿提交 .env 到仓库
# 用于登录脚本生成 auth.json，用例中不要写任何登录信息

# 登录态文件路径（可选，默认项目根目录 auth.json）
# AUTH_FILE=auth.json

# 若登录脚本需要 Cookie（如 wps_sid），可放这里，脚本里用 process.env.WPS_SID 读取
# WPS_SID=your_session_value

# 若使用账号密码登录，可放这里（仅给登录脚本用）
# TEST_USER=your@email.com
# TEST_PASSWORD=your_password
